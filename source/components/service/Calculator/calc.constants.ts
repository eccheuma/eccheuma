import { getLocale, languages } from '~/lang';
import { Second } from '~/types/Nominals';

export const CATEGORIES_LANG = getLocale(languages.Russian).Categories;
export const DAY = 5184000 as Second;